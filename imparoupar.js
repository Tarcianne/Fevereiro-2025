let numero = prompt("escolha um númmero")
function verificarParImpar() {
    if (numero % 2 === 0) {
      alert(numero + " é par");
    } else {
      alert(numero + " é ímpar");
    }
  }
verificarParImpar()